#include <iostream>
#include "datatypes.h"
#include "program.h"

using namespace std;

int main()
{
    // Number n1("5342.2321");
    // Number n2("432.2321");
    // n1 = 45.32;
    // cout << (n1<n2) << (n1>n2) << (n1 - n2 == -386.9121) << endl << (n1-n2).to_str() << endl;
    // // String s = "123";
    // cout <<s<<endl;

    // const Boolean b = false;
    // cout << (!b == b) << endl;

    // Number n1 = 1, n2 = 1;

    // cout << (n2%n1).to_str() << endl;

    // Program p;

    // auto n1 = make_shared<Number>(5);
    // auto n2 = make_shared<Number>(8);
    
    // auto variable = make_shared<shared_ptr<DataType>>();

    // auto v1 = make_shared<ValueExpression>(make_shared<shared_ptr<DataType>>(n1));

    // p.attach_assignment(variable, v1);
    // auto v2 = make_shared<ValueExpression>(make_shared<shared_ptr<DataType>>(n2));
    // auto vv = make_shared<ValueExpression>(variable);
    // auto add = make_shared<BinaryExpression>(vv,v2, BinaryExpression::ADD);
    // auto mult = make_shared<BinaryExpression>(vv,v2, BinaryExpression::MULT);
    // auto sub = make_shared<BinaryExpression>(add,mult, BinaryExpression::SUB);

    // auto n3 = make_shared<Number>(-26);
    // auto v3 = make_shared<ValueExpression>(make_shared<shared_ptr<DataType>>(n3));
    
    // auto cmp = make_shared<BinaryExpression>(sub,v3, BinaryExpression::GT);

    // p.attach_print(cmp);

    // p.run();
}